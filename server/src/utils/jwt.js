/**
 * utils/jwt.js — חתימה ואימות של JWT.
 * ⚠️ STUB. דוגמה (jsonwebtoken):
 *   import jwt from "jsonwebtoken";
 *   import { env } from "../config/env.js";
 *   export const sign = (payload) =>
 *     jwt.sign(payload, env.JWT_SECRET, { expiresIn: env.JWT_EXPIRES_IN });
 *   export const verify = (token) => jwt.verify(token, env.JWT_SECRET);
 *
 * ה-payload המומלץ: { sub: user.id, role: user.role }.
 */

// TODO: לממש sign/verify.
