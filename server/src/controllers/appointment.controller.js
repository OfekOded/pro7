/**
 * appointment.controller.js — בקר תורים.
 * ⚠️ STUB. פעולות:
 *   list:   מסונן לפי תפקיד (מטופל=שלו; רופא=שלו; פקיד/מנהל=הכל) + status.
 *   get:    בדיקת הרשאה לצפייה.
 *   create: { doctorId, departmentId, date, time, reason } → appointmentService.book
 *           (כולל אימות זמינות/double-booking).
 *   update: ביטול/שינוי סטטוס בכפוף לכללים (ביטול עד 24ש לפני, רק 'scheduled').
 */

// TODO: לממש את הפעולות.
