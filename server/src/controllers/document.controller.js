/**
 * document.controller.js — בקר מסמכים.
 * ⚠️ STUB. פעולות:
 *   upload: req.file (מ-multer) + { patientId, visitId? } → documentService.save (מטא ב-DB).
 *   get:    החזרת מטא/הורדת קובץ (בדיקת הרשאה — בעל התיק/רופא/מנהל).
 *   remove: מחיקת רשומה + קובץ מהדיסק.
 */

// TODO: לממש את הפעולות.
