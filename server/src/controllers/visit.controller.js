/**
 * visit.controller.js — בקר ביקורים.
 * ⚠️ STUB. פעולות:
 *   create: { appointmentId, diagnosis, summary, prescriptions[] } →
 *           visitService.close (יוצר visit, מסמן תור 'completed', שומר מרשמים — transaction).
 *   get:    שליפת ביקור בודד.
 */

// TODO: לממש את הפעולות.
