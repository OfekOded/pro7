/**
 * visit.routes.js — ביקורים (תיעוד ע״י רופא/ה).
 *
 * ⚠️ STUB. נתיבים:
 *   POST /api/visits        → visitController.create  (auth + doctor) — "סגור ביקור"
 *   GET  /api/visits/:id    → visitController.get      (auth)
 *
 * הערה: יצירת visit מסמנת את התור כ-'completed' ושומרת מרשמים מקושרים
 *       (transaction). ראו services/visit.service.js.
 */

// TODO: לממש את הנתיבים למעלה.
