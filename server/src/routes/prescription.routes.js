/**
 * prescription.routes.js — מרשמים.
 *
 * ⚠️ STUB. נתיבים:
 *   POST   /api/prescriptions       → prescriptionController.create (auth + doctor)
 *   PATCH  /api/prescriptions/:id    → prescriptionController.update (auth + doctor; active/דוסאז׳)
 *   DELETE /api/prescriptions/:id    → prescriptionController.remove (auth + doctor)
 */

// TODO: לממש את הנתיבים למעלה.
