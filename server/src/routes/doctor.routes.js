/**
 * doctor.routes.js — רופאים, זמינות ואג׳נדה.
 *
 * ⚠️ STUB. נתיבים:
 *   GET  /api/doctors?departmentId=        → doctorController.list        (auth)
 *   GET  /api/doctors/:id                  → doctorController.get         (auth)
 *   GET  /api/doctors/:id/availability?date= → doctorController.availability (auth)
 *   GET  /api/doctors/me/agenda?date=      → doctorController.agenda      (auth + doctor)
 *   GET  /api/doctors/me/availability      → doctorController.myAvailability (auth + doctor)
 *   PUT  /api/doctors/me/availability      → doctorController.setAvailability (auth + doctor)
 *
 * הערה: חישוב החלונות הפנויים (availability) מתבסס על doctor_availability
 *       בניכוי תורים קיימים — ראו services/doctor.service.js.
 */

// TODO: לממש את הנתיבים למעלה.
