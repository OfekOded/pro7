/**
 * document.routes.js — מסמכים (העלאת מדיה).
 *
 * ⚠️ STUB. נתיבים:
 *   POST /api/documents       → documentController.upload (auth + upload.single("file"))
 *   GET  /api/documents/:id    → documentController.get    (auth; הורדה/מטא)
 *   DELETE /api/documents/:id  → documentController.remove (auth)
 *
 * הערה: ההעלאה עוברת דרך middleware/upload.js (multer). הקובץ נשמר ב-UPLOAD_DIR
 *       והמטא נשמר בטבלת documents.
 */

// TODO: לממש את הנתיבים למעלה.
