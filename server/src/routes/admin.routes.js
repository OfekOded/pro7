/**
 * admin.routes.js — ניהול וניתוחים (כל הנתיבים auth + requireRole('admin')).
 *
 * ⚠️ STUB. נתיבים:
 *   GET /api/admin/stats?range=month|quarter|year → adminController.stats
 *       (כרטיסי נתונים, תורים לפי מחלקה, התפלגות סטטוס, עומס לפי רופא/ה)
 */

// TODO: לממש את הנתיב למעלה.
