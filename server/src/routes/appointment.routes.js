/**
 * appointment.routes.js — תורים.
 *
 * ⚠️ STUB. נתיבים:
 *   GET   /api/appointments?status=  → appointmentController.list   (auth; מסונן לפי משתמש/תפקיד)
 *   GET   /api/appointments/:id      → appointmentController.get    (auth)
 *   POST  /api/appointments          → appointmentController.create (auth + patient)
 *   PATCH /api/appointments/:id      → appointmentController.update (auth; ביטול/סטטוס)
 *
 * הערה: ביצירה — אימות זמינות ומניעת double-booking (UNIQUE doctor/date/time
 *       + בדיקה בשירות). ביטול — רק בסטטוס 'scheduled' ועד 24ש לפני.
 */

// TODO: לממש את הנתיבים למעלה.
