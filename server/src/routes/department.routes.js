/**
 * department.routes.js — מחלקות.
 *
 * ⚠️ STUB. נתיבים:
 *   GET    /api/departments        → departmentController.list   (auth)
 *   POST   /api/departments        → departmentController.create (admin)
 *   PUT    /api/departments/:id     → departmentController.update (admin)
 *   DELETE /api/departments/:id     → departmentController.remove (admin)
 */

// TODO: לממש את הנתיבים למעלה.
