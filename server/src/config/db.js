/**
 * config/db.js — חיבור ל-MySQL באמצעות connection pool (mysql2/promise).
 *
 * ⚠️ STUB. אחריות: ליצור pool יחיד שכל ה-models משתמשים בו.
 *
 * דוגמה:
 *   import mysql from "mysql2/promise";
 *   import { env } from "./env.js";
 *   export const pool = mysql.createPool({
 *     host: env.DB.host, port: env.DB.port,
 *     user: env.DB.user, password: env.DB.password, database: env.DB.database,
 *     waitForConnections: true, connectionLimit: 10, namedPlaceholders: true,
 *   });
 *
 *   // שימוש ב-model:  const [rows] = await pool.query("SELECT ...", params);
 */

// TODO: לממש לפי הדוגמה.
export const pool = null;
