/**
 * config/env.js — טעינה וריכוז של משתני הסביבה.
 *
 * ⚠️ STUB. אחריות: dotenv.config() ואז לחשוף אובייקט env מטופס/מאומת.
 *
 * דוגמה:
 *   import "dotenv/config";
 *   export const env = {
 *     PORT: Number(process.env.PORT) || 4000,
 *     CLIENT_URL: process.env.CLIENT_URL,
 *     DB: { host, port, user, password, database },
 *     JWT_SECRET: process.env.JWT_SECRET,
 *     JWT_EXPIRES_IN: process.env.JWT_EXPIRES_IN || "7d",
 *     UPLOAD_DIR: process.env.UPLOAD_DIR || "uploads",
 *   };
 *   // מומלץ לאמת שכל המשתנים הקריטיים קיימים ולזרוק שגיאה אם לא.
 */

// TODO: לממש לפי הדוגמה. ראו .env.example לרשימת המשתנים.
export const env = {};
