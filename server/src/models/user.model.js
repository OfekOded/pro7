/**
 * user.model.js — גישת נתונים לטבלת users (mysql2 pool).
 * ⚠️ STUB. שאילתות: findByEmail, findById, findByNationalId, create, update, list, remove.
 * שימוש בפרמטרים מוכנים (?) למניעת SQL injection.
 *
 * דוגמה:
 *   export async function findByEmail(email) {
 *     const [rows] = await pool.query("SELECT * FROM users WHERE email=?", [email]);
 *     return rows[0] ?? null;
 *   }
 */

// TODO: לממש את השאילתות מעל config/db.js (pool).
