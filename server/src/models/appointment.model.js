/**
 * appointment.model.js — גישת נתונים ל-appointments.
 * ⚠️ STUB. שאילתות:
 *   listByPatient, listByDoctor, listAll (עם פילטר status),
 *   findById, create, updateStatus,
 *   takenSlots(doctorId, date) — להחזרת השעות התפוסות (לחישוב זמינות).
 * הערה: ה-create עשוי להיכשל על UNIQUE(doctor,date,time) — לתפוס ולהחזיר 409.
 */

// TODO: לממש.
