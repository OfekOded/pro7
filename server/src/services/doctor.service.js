/**
 * doctor.service.js — לוגיקת רופאים + חישוב זמינות.
 *
 * ⚠️ STUB. אחריות מרכזית:
 *   getAvailableSlots(doctorId, date):
 *     1. לשלוף doctor_availability ליום בשבוע של date.
 *     2. לפרוס לחלונות לפי slot_minutes (start_time..end_time).
 *     3. לסנן חלונות שכבר תפוסים (appointments בסטטוס scheduled לאותו תאריך).
 *     4. להחזיר רשימת חלונות עם דגל taken — בדיוק כפי שהפרונט מצפה (timeSlots).
 *   getAgenda(doctorId, date): ציר התורים של היום + ספירות סטטוס.
 */

// TODO: לממש.
