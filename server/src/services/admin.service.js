/**
 * admin.service.js — אגרגציות לדשבורד הניהול.
 * ⚠️ STUB. אחריות:
 *   stats(range): שאילתות אגרגציה (GROUP BY) שמחזירות:
 *     - cards: סה״כ תורים, אחוז ביטולים, מטופלים פעילים, רופאים פעילים (+ דלתא).
 *     - byDepartment: COUNT תורים לפי מחלקה.
 *     - statusBreakdown: התפלגות סטטוס באחוזים.
 *     - loadByDoctor: COUNT תורים לפי רופא/ה.
 *   המבנה זהה ל-client/src/data/mock.js (adminStats) כדי שהפרונט יתחבר ישירות.
 */

// TODO: לממש.
