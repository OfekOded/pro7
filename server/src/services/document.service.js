/**
 * document.service.js — לוגיקת מסמכים.
 * ⚠️ STUB. אחריות:
 *   save({ file, patientId, visitId, uploadedBy }): לשמור מטא (שם/נתיב/mime/גודל) ב-DB.
 *   remove(id): מחיקת רשומה + מחיקת הקובץ הפיזי מ-UPLOAD_DIR.
 */

// TODO: לממש.
