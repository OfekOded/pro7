/**
 * visit.service.js — סגירת ביקור.
 * ⚠️ STUB. אחריות:
 *   close({ appointmentId, diagnosis, summary, prescriptions[] }) — בתוך transaction:
 *     1. ליצור רשומת visit.
 *     2. לעדכן את התור ל-status='completed'.
 *     3. להכניס מרשמים מקושרים (prescriptions) עם visit_id.
 *   במקרה כשל בכל שלב — rollback.
 */

// TODO: לממש.
