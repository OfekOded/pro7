/**
 * middleware/validate.js — ולידציית קלט לבקשות.
 * ⚠️ STUB. ניתן לממש ידנית או עם ספרייה (zod/joi/express-validator).
 *
 * דוגמה (ידני):
 *   export const validate = (schema) => (req, res, next) => {
 *     const { error, value } = schema(req.body);   // schema מחזיר {error, value}
 *     if (error) return next(new ApiError(422, "קלט לא תקין", error));
 *     req.body = value;
 *     next();
 *   };
 */

// TODO: לבחור גישת ולידציה ולממש.
