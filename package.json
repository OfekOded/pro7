{
  "name": "clinic-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "מרפאה חכמה — מערכת ניהול מרפאה וזימון תורים (פרויקט גמר Full-Stack). מונורפו: client (React/Vite) + server (Node/Express/MySQL).",
  "scripts": {
    "client": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:lint": "npm --prefix client run lint",
    "server": "npm --prefix server run dev",
    "install:all": "npm --prefix client install && npm --prefix server install",
    "db:schema": "npm --prefix server run db:schema",
    "db:seed": "npm --prefix server run db:seed"
  }
}
